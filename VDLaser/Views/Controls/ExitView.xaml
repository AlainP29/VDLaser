<UserControl x:Class="VDLaser.Views.Controls.ExitView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VDLaser.Views.Controls"
        mc:Ignorable="d">
    <Grid>
        <Border BorderBrush="LightBlue" BorderThickness="1" Margin="10">
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center"
                            VerticalAlignment="Top" >
                <Button Width="75" Margin="5" Content="{DynamicResource ExitButton}" 
                        Style="{StaticResource ControlStyleVD}" Height="40"
                            Command="{Binding QuitApplicationCommand, Mode=OneWay}"/>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>

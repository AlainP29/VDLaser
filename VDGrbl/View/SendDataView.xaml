<UserControl x:Class="VDGrbl.View.SendDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VDGrbl.Tools"
             mc:Ignorable="d" Height="120" Width="300">
    <UserControl.Resources>
        <local:InverseBooleanConverter x:Key="ConvertBool"/>
        <local:DoubleConverter x:Key="ConvertDouble"/>
    </UserControl.Resources>
    <!--It shows all the data sent manually/Jog/buttons to the Arduino. no stack, new data erase old data -->
    <Grid DataContext="{Binding Source={StaticResource Locator}}">
        <GroupBox Header="Send data" Style="{StaticResource GroupBoxStyleVD}" IsEnabled="{Binding Main.IsSelectedKeyboard,Converter={StaticResource ConvertBool}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBox Height="23" Margin="5" TextWrapping="Wrap" VerticalAlignment="Center" VerticalContentAlignment="Center" Text="{Binding Main.TXLine, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120">
                        <TextBox.InputBindings>
                            <KeyBinding Command="{Binding Main.SendCommand}" Key="Return"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button  Content="Send" HorizontalAlignment="Left" Margin="5" VerticalAlignment="Center" Width="75" Command="{Binding Main.SendCommand, Mode=OneWay}"/>
                    <TextBox  Text="{Binding Main.ResponseStatus, Mode=OneWay}" Margin="5" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="30" VerticalAlignment="Center" IsReadOnly="True" IsEnabled="False"/>
                </StackPanel>
                <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Maximum="{Binding Main.MaxPlannerBuffer}" Value="{Binding Main.PlannerBuffer}" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="150"/>
                    <TextBlock Text="{Binding Main.PlannerBuffer, ConverterParameter=100, Converter={StaticResource ConvertDouble}, StringFormat=\{0:P0\}}" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" />
                </Grid>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
